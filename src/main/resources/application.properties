spring.datasource.url=jdbc:${MySQL:mariadb}://${Conexao:127.0.0.1}:3306/zup_propostas

spring.datasource.username=root
spring.datasource.password=zupper

spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1

#spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.hibernate.naming-strategy=org.hibernate.cfg.ImproveNamingStrategy
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

# actuator
management.endpoint.health.show-details=always
management.endpoints.web.exposure.include=*
info.app.name=@project.name@
info.app.version=@project.version@

# spring boot admin server
#spring.boot.admin.client.url=http://localhost:8081

service.analiseSolicitacao.url=${SERVICE_ANALISE_SOLICITACAO:http://localhost:9999/api/solicitacao}
service.cartao.url=${SERVICE_CARTAO:http://localhost:8888}

## spring security resource server configuration
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI:http://localhost:18080/auth/realms/propostas}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${KEYCLOAK_JWKS_URI:http://localhost:18080/auth/realms/propostas/protocol/openid-connect/certs}